# Requirements for FastAPI application with SQL Server connectivity

# Core dependencies
python-dotenv==1.2.1

# Web framework
fastapi==0.136.0
uvicorn==0.44.0

# Database connectivity
sqlalchemy==2.0.49 # For database interactions
databases==0.9.0 # For async database support
pyodbc==5.3.0

# Data validation
pydantic==2.12.5 # For data validation and settings management
email-validator==2.3.0 # For validating email addresses with Pydantic